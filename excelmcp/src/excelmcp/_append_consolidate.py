"""Deprecated placeholder.

This module previously contained a one-off developer script that appended code
into server_io.py at import time. The implementation has been removed so the
module is inert if imported accidentally.
"""
